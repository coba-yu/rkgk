## Method

Every paper is split into page-aligned chunks and embedded with a sentence encoder.
Graph-guided retrieval is a form of retrieval-augmented generation in which the graph walk adds papers the retriever missed.

## Task

We address next-paper recommendation: given a theme, the system names the paper to read next.
